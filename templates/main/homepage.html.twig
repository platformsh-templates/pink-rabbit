{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user %}
        <div class="col-12 mb-5">
            <h2 class="h2 mb-2 font-weight-normal"><span>Al</span>ice, congratulations!</h2>
            {% if nopouc %}
            {% endif %}
            <p class="drink-me">Now, go back to the booth with the <strong>title</strong> of the <strong>last post</strong> on <strong><a href="https://blog.blackfire.io/">Blackfire Blog</a></strong> to enter the raffle to win a prize in top of that!</p>
            <p class="drink-me">You have discovered Blackire Profiler. Continue your journey on <strong><a href="https://demo.blackfire.io/">demo.blackfire.io</a></strong> to discover <strong>all Blackfire features</strong> including live Monitoring.</p>
        </div>
    {% endif %}
    <div class="col-8">
        <h3>Recently Reported Sightings</h3>
        <small>Submit yours to help us narrow down this search</small>
        <div class="divTable table table-striped table-dark table-borderless table-hover">
            <div class="divTableHeading">
                <div class="divTableRow bg-info">
                    <div class="divTableHead">Sighting Details</div>
                    <div class="divTableHead">Posted</div>
                    <div class="divTableHead">Comments</div>
                </div>
            </div>
            <div class="divTableBody js-sightings-list" data-url="{{ path('app_sightings_partial_list') }}">
                {{ include('main/_sightings.html.twig') }}
            </div>
        </div>
    </div>
    <div class="col-4">
        <div class="js-github-organization" data-url="{{ path('app_github_organization_info') }}"></div>
    </div>
{% endblock %}
